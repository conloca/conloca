/**
 * Re-export ImageFieldRender as ImageUrlField for backwards compatibility.
 * Both components were functionally identical — text input with media library browse button.
 */
export { ImageFieldRender as ImageUrlField } from './ImageField';
