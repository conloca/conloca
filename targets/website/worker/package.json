{
  "name": "conloca-subscribe-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply conloca-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply conloca-db --remote"
  },
  "devDependencies": {
    "wrangler": "^3.101.0"
  }
}
